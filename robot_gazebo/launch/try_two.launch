<launch>
  <include file="$(find gazebo_ros)/launch/empty_world.launch">
    <arg name="world_name" value="$(find robot_gazebo)/world/try_two.world"/>
    <arg name="paused" value="false"/>
    <!-- more default parameters can be changed here -->
  </include>
</launch>